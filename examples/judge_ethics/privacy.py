from __future__ import annotations

import prompt_craft_kit as pck


pck.kv(
    {
        "PII": "Avoid collecting unnecessary personal information",
        "minimization": "Only use data directly relevant to the decision",
        "retention": "Prefer ephemeral processing; avoid storing unless required",
    }
)
