[pytest]
minversion = 6.0
pythonpath = src
testpaths = tests
addopts = -vvv --cov=. --cov-report xml --cov-fail-under=80
asyncio_mode = auto
log_cli = true
log_cli_level = DEBUG
log_cli_format = %(asctime)s - %(name)s - %(levelname)s - %(message)s
